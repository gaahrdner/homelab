---
# 1Password Item reference for WordPress database credentials
# IMPORTANT: Before deploying, add these credentials to the 1Password kubernetes vault:
#   - Item name: wordpress-db-credentials
#   - Fields: password (DB user password), root-password (MariaDB root password)
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: wordpress-db-credentials
  namespace: texasdust
spec:
  itemPath: "vaults/mbn7rk3d5gfgofrv3a2zt2tdoe/items/wordpress-db-credentials"
