apiVersion: v1
kind: Service
metadata:
  name: paperclip
  namespace: paperclip
spec:
  selector:
    app.kubernetes.io/name: paperclip
  ports:
    - name: http
      port: 3100
      targetPort: http
