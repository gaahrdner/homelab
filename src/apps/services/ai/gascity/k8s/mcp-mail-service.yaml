apiVersion: v1
kind: Service
metadata:
  name: mcp-mail
  namespace: gascity
  labels:
    app: mcp-mail
    app.kubernetes.io/part-of: gascity
spec:
  type: ClusterIP
  ports:
    - port: 8765
      targetPort: 8765
      protocol: TCP
      name: http
  selector:
    app: mcp-mail
