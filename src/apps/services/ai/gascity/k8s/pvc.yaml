apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gascity-data
  namespace: gascity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
