apiVersion: v1
kind: Service
metadata:
  name: dolt
  namespace: gascity
  labels:
    app: dolt
    app.kubernetes.io/part-of: gascity
spec:
  type: ClusterIP
  selector:
    app: dolt
  ports:
    - name: mysql
      port: 3307
      targetPort: mysql
      protocol: TCP
