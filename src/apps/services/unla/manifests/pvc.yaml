apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unla-data
  namespace: unla
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
